The runtime uses a verified Roblox-compatible atlas rather than copying the full upstream SVG collection into this repository.

The exact upstream icon names, Lucide version, image IDs, and sprite coordinates are recorded in `../manifest.json`. To regenerate or replace the subset from official SVG source, use `tools/build_icons.py`.
